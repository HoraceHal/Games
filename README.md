# 数据驱动版 + 搜索/筛选/排序 + 主题切换
- 左上角搜索：按标题模糊匹配
- 标签筛选：全部 / 推荐 / 最新（最新自动按日期）
- 排序：默认 / 日期（新→旧）/ 浏览量（高→低）
- 卡片显示：封面、状态徽标、标题、分类行、日期与浏览量
- 主题切换：右上角白天/夜间，并持久化到 localStorage
- 数据源：`games.json`（支持 `tags`, `views`, `category`, `status`）
