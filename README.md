# Polish 版：可配置标签颜色 + 新品提示 + 更强悬停动效 + 健壮性
- `games.json` 支持 `statusColor` 自定义标签颜色；
- `tags` 含 `最新` **或** `date` 在 30 天内 → 显示左上角 `新` 徽标；
- 悬停：卡片上浮 + 封面轻缩放，阴影柔和；
- 健壮性：容错解析 `games.json`（去注释/尾逗号），图片失败自动换占位。
